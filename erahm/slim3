slim3:
    description: Authentication API for Snowcap Digital
    require:
        - kcmerrill/automagicproxy
    image: erahm/slim3
    remove: true
    build: src/
    name: ${PWD##*/}
    P: true
    setup:
        - git clone git@github.com:erahm/slim3-docker.git src && cd src/ && git checkout master && ./app/composer.phar install
    v:
        - $PWD/src/app/:/var/www/html
